# Google Cloud Project Configuration
GOOGLE_CLOUD_PROJECT=your-project-id-here
GOOGLE_PROJECT_ID=your-project-id-here

# Google Cloud Credentials Path (default location for macOS/Linux)
# Update this path if your credentials are in a different location
GOOGLE_APPLICATION_CREDENTIALS=${HOME}/.config/gcloud/application_default_credentials.json

# Optional: Custom credentials path
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/credentials.json

# HTTP Server Configuration
MCP_HOST=0.0.0.0
MCP_PORT=8000

# SSE Server Configuration (for SSE transport)
MCP_SSE_HOST=0.0.0.0
MCP_SSE_PORT=8000

# Allow requests from any host (for proxies, tunnels, etc.)
ALLOWED_HOSTS=*